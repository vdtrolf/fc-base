{
  "main": "dist/app.js",
  "scripts": {
    "compile": "tsc --project ./tsconfig.json",
    "start": "node dist/index.js",
    "lint": "eslint . --ext .ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "acebase": "^1.26.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^4.9.4"
  }
}
